{
  "name": "taskboard-server",
  "version": "1.0.0",
  "private": true,
  "description": "Local sync backend for the Offline-First Task Board iOS app",
  "type": "module",
  "engines": { "node": ">=18.17" },
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "better-sqlite3": "^12.2.0",
    "express": "^5.1.0"
  }
}
